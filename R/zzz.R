# .onLoad doesn't really load rJava.
# will have to do it from the function itself

# .onLoad <- function(libname, pkgname) {
#     # rJava::.jpackage(pkgname, lib.loc = libname)
#     # print(rJava::.jclassPath())
# }
